import type { Metadata } from "next";import { PageHero } from "@/components/page-hero";import { processSteps } from "@/lib/content";
export const metadata:Metadata={title:"Our Process",description:"How Flowcoat reviews, prepares, coats, cures and checks suitable metal parts.",alternates:{canonical:"/process"}};
export default function Process(){return <main id="main"><PageHero label="Our process" title="Controlled steps. Job-specific decisions." intro="Metal type, mass, geometry, previous coating, intended environment and powder specification can all change the required method. Not every part follows an identical process."/><section className="wrap py-16">{processSteps.map(([n,t,c])=><article key={n} className="grid gap-4 border-t border-[var(--line)] py-8 sm:grid-cols-[5rem_1fr_1.2fr]"><span className="eyebrow text-[var(--brand-dark)]">{n}</span><h2 className="text-3xl font-black">{t}</h2><p className="max-w-xl text-lg leading-relaxed text-[var(--muted)]">{c}</p></article>)}</section></main>}
